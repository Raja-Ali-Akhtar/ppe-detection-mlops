[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E402"]   # sys.path juggling before imports is deliberate in scripts/tests

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
